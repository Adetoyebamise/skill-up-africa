const eventEmitter = require("events");
const loggerEmitter = new eventEmitter();
// function logActivity( ) {
//   process.stdout.write()
// }
// logActivity();
// loggerEmitter.on("event", () => {
// process.stdout.write("message", (data) => {
// ` Please Drop your message : \n ${data.toString().trim()}`;
// });
// console.log(`Scan your fingerprint, You are logged in at : ${new Date()}`);
// });
// process.stdout.on("data", (data) => {
//   process.stdout.write(` Please Drop your message : \n ${data}`);
//   process.exit();
// });
// loggerEmitter.emit("event");

// const event = loggerEmitter
//   .getDefaultCalendar()
//   .createAllDayEvent("Apollo 11 Landing", new Date("July 20, 1969"), {
//     location: "The Moon",
//   });
// Logger.log("Event ID: " + event.getId());
function createAllDayEvent(title, date) {
  console.log(`Evening Node classes, ${new Time()}`);
}
